import styled from "@emotion/styled";

export const Ul = styled.ul({
  height: "100%",
  margin: 0,
  display: "flex",
  alignItems: "center",
});

export const Li = styled.li({
  listStyle: "none",
  marginLeft: 20,
  fontSize: 20,
});
