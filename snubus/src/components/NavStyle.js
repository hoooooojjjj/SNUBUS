import styled from "@emotion/styled";

// ul 태그
export const Ul = styled.ul({
  height: "100%",
  margin: 0,
  display: "flex",
  alignItems: "center",
});

// li 태그
export const Li = styled.li({
  listStyle: "none",
  marginLeft: 20,
  fontSize: 20,
  cursor: "pointer",
});
